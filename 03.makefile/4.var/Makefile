#变量

A := $C #即时变量，定义时确定
B = $C #延时变量，使用时才确定
C = 2

D = xunxun
D ?= haha #这句不会起效果

all:
	@#在命令前加@符号，输出时不会输出命令
	@echo A=$A 
	@echo B=$B
	@echo D=$D

#输出时A没有值，因为A确定时C还没有值
#B因为延时确定，所以有值

#:= 即时变量
#= 延时变量
#?= 延时变量，如果时第一次定义才起效，如果在前面该变量已定义则忽略这句
#+= 附加